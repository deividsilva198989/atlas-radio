const player = document.getElementById('player'); const btn = document.getElementById('btnPlay'); const statusText = document.getElementById('status'); const ouvintes = document.getElementById('ouvintes'); btn.addEventListener('click', async () => { if(player.paused){ await player.play(); btn.innerText = '⏸ PAUSAR'; statusText.innerText = 'Ao vivo'; }else{ player.pause(); btn.innerText = '▶ OUVIR AGORA'; statusText.innerText = 'Pausado'; } }); setInterval(() => { ouvintes.innerText = Math.floor(Math.random() * 200) + ' ouvintes'; }, 5000); const mensagens = document.getElementById('mensagens'); document.getElementById('enviar').onclick = () => { const nome = document.getElementById('nome').value.trim(); const recado = document.getElementById('recado').value.trim(); if(!nome || !recado) return; const p = document.createElement('p'); p.innerHTML = `<strong>${nome}:</strong> ${recado}`; mensagens.prepend(p); document.getElementById('recado').value = ''; };